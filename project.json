{
  "goal": "A Python CLI tool that scans a specified directory for image files (PNG, JPG) and PDF files. It uses a local OCR library to extract text, then applies regex rules to identify vendor, date, and amount fields. It outputs a structured CSV/JSON file and supports simple tax categorization rules. No external APIs or cloud sync.",
  "acceptance_criteria": [
    "Module runs via python command",
    "Regex patterns extract vendor, date, amount correctly",
    "CSV output generated"
  ],
  "meeting_budget": 5,
  "meetings_held": 0,
  "status": "active"
}